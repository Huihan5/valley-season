/// <reference types="vite/client" />

// Brings in Vite's module declarations for asset imports — without it, the map's
// `import mapUrl from '../../assets/map_valehold.png'` has no type and tsc refuses it.
