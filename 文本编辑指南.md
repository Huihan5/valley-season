# 文本编辑指南 · Editing the game's text

游戏里所有能被玩家看到的文字都散在 `src/data/zh/**` 和 `src/data/en/**` 的 55 对 JSON 里。直接改 JSON 容易出错（引号、逗号、键名、中英不同步），也看不出某句话"用在哪"。

这套工具把**全部文本**摊平成一个可读、可改的镜像文件夹 `text-review/`，中英并排、带上下文，改完一键写回。**`src/data` 始终是唯一真相**；`text-review/` 只是给你改字用的工作台，随时能重新生成。

---

## 三步工作流

```bash
npm run text:export   # ① 生成/刷新 text-review/（从 src/data 摊平）
#                     ② 在 text-review/ 里改字（见下）
npm run text:import   # ③ 把改动写回 src/data，并做基础校验
npm test              # ④ 最终确认（576 项，含中英配对/标点/占位）
```

> 也可以只做第 ②③ 步之外的一半：**你在 `text-review/` 里改，然后叫我（Claude）跑 import 写回**——就是你说的"我改、你填回去"。我会顺手把校验和 `npm test` 也跑了。

> **只改中文也行（推荐的分工）**：你只改 `〖zh〗`，`〖en〗` 原样别动；改完告诉我，我在 import 前把你改过的每段中文**翻译 + 按 humanizer 润色成英文**（弯撇号、段数、占位符都对齐）再写回，然后 import + `npm test`。净效果：**你永远只写中文。** 详见下面「只改中文」。

**顺序很重要**：`text-review/` 没有进 git（可再生）。如果你改了一半还没 import，**不要**先 `text:export`——那会用 `src/data` 覆盖掉你没保存的改动。规矩是：**先 import 落盘，再 export 刷新**。

---

## 怎么改字

打开 `text-review/INDEX.md`，那是总目录（按 序章 / 对话 / 场景 / 事件 / 结局 分组，每个文件写了"是什么、多少段"）。点进对应的 `.md`，每段文本长这样：

```
<!--k:choices.0.resultText-->
### `choices.0.resultText`
_选项 1 的结果文字 · choice 1 result_

〖zh〗
你把那张收条推到她面前。
〖en〗
You slide the receipt across to her.
〖end〗
```

**只改 `〖zh〗` 和 `〖en〗` 之间的正文。** 三行标记 `〖zh〗` / `〖en〗` / `〖end〗` 和上面的 `<!--k:...-->`、`###`、`_..._` 都别动——它们负责告诉 import 这段该写回哪。

- **中英一起改，或只改中文（见下）。** 测试强制中英成对，所以英文迟早要跟上——要么你顺手改了，要么留给我。
- **多段落**照原样空行分段即可，中英段数要一样。
- **占位符**（`{playerName}`、`{grain}`、`{n}` 之类）中英都要原样保留、数量一致。

### 只改中文（把英文交给我）

日常最省事的分工：

1. 只改 `〖zh〗` 里的中文，**`〖en〗` 原样别动**（留着旧英文当参考；清空也行，我会补）。
2. 改完告诉我。我会先 `text:import` 看它报告哪些中文变了（它还会提示"这段英文和新中文对不上"），据此把**每段改过的中文翻译 + 按 humanizer 润色成英文**——弯撇号 `’`、段落数、占位符都对齐——写回 `〖en〗`，再 import 一次，最后 `npm test`。
3. 净效果：**你永远只写中文，英文由我保证与中文同步且符合规范。**

> 注意：如果你只改了中文就直接自己跑 `text:import`，英文不会自动更新——JSON 里会是"新中文 + 旧英文"，测试可能仍然绿（它查的是结构配对，不查语义），但英文其实过时了。所以只改中文这条路，import 前的翻译润色一定要走一遍（交给我最稳）。

---

## 标点规范（会被测试和 import 检查）

- **中文**：只用全角中文标点 `，。、；：？！` 和中文引号 `“ ” ‘ ’`。**不要**用角括号 `「」`、也不要用半角 `, . ; :` 或直引号 `" '`。
- **英文**：撇号用弯的 `’`（`don’t`、`Marigni’s`）——直的 `'` 会让 `Localization.test` 报错；英文里不能出现中文字符。

`npm run text:import` 会在写回后逐段检查这些，并列出问题（**改动仍会写入**，方便你对着提醒修；`npm test` 是最终裁判）。

---

## 哪些文字在哪（domain map）

| 文件夹 / 文件 | 内容 |
|---|---|
| `opening.md` | 开场序章 |
| `ui.md` · `system_lines.md` | 界面按钮、日期、系统提示（很少需要动） |
| `actions.md` | 每个行动（收割、砍木、休息…）的结果文字 |
| `codex.md` | 见闻（人物 + 世界知识） |
| `dialogue/greetings.md` | NPC 招呼语，按信任阶（estranged→embraced）分组 |
| `dialogue/fragments.md` | NPC 对话片段 |
| `scenes/locations.md` · `ambient.md` · `weather_lines.md` · `rumors.md` · `market.md` · `action_results.md` | 地点描写、氛围、天气、传闻、集市、行动场景 |
| `events/dayN*.md` | 各天的固定事件（标题写了 Day 几 · 时段 · 事件名） |
| `events/random/*.md` | 随机事件池 |
| `endings/endings.md` | 五个结局脚本 |

每段上面那行 `_..._` 会说明它的角色（开场场景 / 选项按钮 / 选项结果 / 卷宗日志 / 场景变体 / 见闻分层…）。

---

## 有意排除的东西

摊平时**跳过了逻辑锚点**——不是给人读的文字：条目 `id`、跳转 `next`、时段 `timing`、旗标名、猎季/晚宴的选项字母（`A`/`B`/`C`）等。它们留在 JSON 里不动。你在 `text-review/` 里看到的，都是真正会显示给玩家的文本。

想改这些结构性的东西（新增一段、改分支、改旗标）属于**改代码**，不走这套工具——直接说，我来改 `src/data` 的 JSON 和对应 `systems/`。

---

## 保证与边界

- **无损往返**：不改任何字就 `export` 再 `import`，`src/data` 零变化（已验证）。import 只重写**确有改动**的字符串，其它一字不动，键序、缩进、结构保持原样，git diff 干净。
- **单一真相**：`text-review/` 在 `.gitignore` 里，可随时 `text:export` 重建。要让改动"留下来"，靠的是 import 写回 `src/data` 后**提交 `src/data`**，不是提交 `text-review/`。
- 工具本体：`scripts/text-export.mjs` / `scripts/text-import.mjs`（已进 git）。
